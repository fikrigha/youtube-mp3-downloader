## Usage

1. Download code base then run

   ```sh
   npm install
   ```

1. Run the script:

   ```sh
   npm start
   ```

1. Enter the YouTube URL when prompted:

   ```sh
   Enter the YouTube URL: <YouTube URL>
   ```
